mixin checkbox(name, model, label)
    div(class="form-group")
        div(class="form-check")
            label(class="form-check-label") 
                input(type="checkbox" class="form-check-input" name=`${name}` ng-model=`${model}`)
                span= ` ${label}`